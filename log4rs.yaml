appenders:
  stdout:
    kind: console

root:
  level: info
  appenders:
    - stdout

loggers:
  wikidata_realtime_dumps:
    level: info
  actix_server:
    level: info
  actix_web:
    level: info

