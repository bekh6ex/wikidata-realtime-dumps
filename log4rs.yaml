refresh_rate: 30 seconds

appenders:
  stdout:
    kind: console

root:
  level: info
  appenders:
    - stdout

loggers:
  wikidata_realtime_dumps:
    level: info
  wikidata_realtime_dumps::get_entity:
    level: debug

